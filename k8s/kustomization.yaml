apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - deployment.yaml
  - service.yaml
# secret.example.yaml is intentionally not listed here — generate the real
# secret out-of-band (kubectl create secret / SealedSecret), never apply the
# example file as-is.
